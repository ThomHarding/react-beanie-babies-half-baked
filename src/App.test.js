// import { render, screen } from '@testing-library/react';
// import App from './App';

test('starts on page 1', () => {
  // render(<App />);
  // const linkElement = screen.getByText('Current Page 1');
  // expect(linkElement).toBeInTheDocument();
});
